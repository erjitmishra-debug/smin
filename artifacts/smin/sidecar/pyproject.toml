[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "smin-sidecar"
version = "0.1.0"
description = "SMIN Phase 1 validation sidecar foundation"
requires-python = ">=3.12"

[project.scripts]
smin-sidecar = "smin_sidecar:main"